import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function StaffAdminPage() {
  return <Card><CardHeader><CardTitle>Admin</CardTitle><CardDescription>Hospital account verification, staff permissions, and ward configuration.</CardDescription></CardHeader><CardContent>Mock admin workspace for Medical Staff RBAC.</CardContent></Card>;
}